import {NewPasswordForm} from "@/components/NewPassword/new-password-form";
import {Center} from "@mantine/core";


const NewPasswordPage = () => {
    return (
        <Center style={{height: '100vh'}}>
            <NewPasswordForm/>
        </Center>
    );
};

export default NewPasswordPage;
